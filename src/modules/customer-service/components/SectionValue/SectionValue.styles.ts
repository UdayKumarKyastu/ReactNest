import styled from '@emotion/styled'
import tw from 'twin.macro'

const Label = styled.label`
  ${tw`block text-lg mb-4`}
`

const Value = styled.div`
  ${tw`text-base`}
`

export const SectionValueStyles = {
  Label,
  Value,
}
