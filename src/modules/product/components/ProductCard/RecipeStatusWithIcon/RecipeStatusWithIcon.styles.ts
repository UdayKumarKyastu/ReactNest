import styled from '@emotion/styled'
import tw from 'twin.macro'

const RecipeStatusWrapper = styled('div')`
  svg {
    ${tw`mr-1`}
  }
`

export const RecipeStatusWithIconStyles = {
  RecipeStatusWrapper,
}
