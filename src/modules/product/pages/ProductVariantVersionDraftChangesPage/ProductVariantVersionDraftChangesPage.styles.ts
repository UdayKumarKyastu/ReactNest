import styled from '@emotion/styled'
import tw from 'twin.macro'

const FormButtonsWrapper = styled('div')`
  ${tw`flex flex-row justify-end mt-10`};
`

export const ProductVariantVersionDraftChangesPageStyles = {
  FormButtonsWrapper,
}
