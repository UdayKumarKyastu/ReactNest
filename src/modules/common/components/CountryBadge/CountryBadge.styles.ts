import styled from '@emotion/styled'
import tw from 'twin.macro'

const Wrapper = styled.div`
  ${tw`flex items-center`}
`

const Label = styled.p`
  ${tw`ml-1 text-sm`}
`

export const CountryBadgeStyles = {
  Wrapper,
  Label,
}
