import styled from '@emotion/styled'
import tw from 'twin.macro'

const Wrapper = styled.div`
  ${tw`w-full py-8 px-24`}
`

const Title = styled.h1`
  ${tw`text-3xl font-bold mb-8`}
`

export const PricingImportStyles = {
  Wrapper,
  Title,
}
