import styled from '@emotion/styled'
import tw from 'twin.macro'

const Wrapper = styled.div`
  ${tw`flex items-center`}

  button {
    ${tw`text-base`}
  }
`

export const TemplateFileInfoStyles = {
  Wrapper,
}
