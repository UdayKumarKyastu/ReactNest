import styled from '@emotion/styled'
import tw from 'twin.macro'

const Header = styled.header`
  ${tw`flex justify-between items-center`}
`

export const RecipeCalculatorCreateNewStyles = {
  Header,
}
